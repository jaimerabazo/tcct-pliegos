node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
coverage

/src/generated/prisma
.vercel

# Caché de graphify: artefactos regenerables (AST + análisis semántico por fichero).
# El grafo en sí (GRAPH_REPORT.md, graph.json, graph.html) sí se versiona.
graphify-out/cache/
graphify-out/cost.json
graphify-out/2026-07-28

.env*
# La plantilla SÍ se versiona (no lleva secretos); la negación debe ir DESPUÉS del glob
!.env.example
